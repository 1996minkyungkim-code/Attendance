# 구글 스프레드시트 연동 가이드

## 1. 스프레드시트 준비
1. 새 구글 스프레드시트를 만듭니다.
2. 시트 이름을 `출퇴근기록`으로 변경합니다 (또는 아래 코드의 시트 이름을 맞게 수정).
3. 1행에 헤더를 넣습니다: `날짜 | 시간 | 과정 | 이름 | 구분`

## 2. Apps Script 작성
스프레드시트에서 `확장 프로그램 > Apps Script`를 열고 아래 코드를 붙여넣습니다.

```javascript
function doPost(e) {
  var sheet = SpreadsheetApp.getActiveSpreadsheet().getSheetByName('출퇴근기록');
  var data = JSON.parse(e.postData.contents);
  var courseLabel = data.course === 'phd' ? '박사과정생' : '석사과정생';
  var actionLabel = data.action === 'in' ? '출근' : '퇴근';

  sheet.appendRow([data.date, data.time, courseLabel, data.name, actionLabel]);

  return ContentService
    .createTextOutput(JSON.stringify({ result: 'success' }))
    .setMimeType(ContentService.MimeType.JSON);
}
```

## 3. 배포
1. Apps Script 편집기 우측 상단 `배포 > 새 배포`
2. 유형: `웹 앱`
3. 실행 계정: `나(본인)`
4. 액세스 권한: `모든 사용자` (키오스크에서 로그인 없이 호출하기 위함)
5. 배포 후 나오는 `웹 앱 URL`을 복사합니다.

## 4. 웹페이지에 연결
- 출퇴근 키오스크 페이지의 Tweaks(조정) 패널을 열어 `webhookUrl`에 위 URL을 붙여넣습니다.
- `testMode`를 꺼주면 실제로 스프레드시트에 기록이 전송됩니다. (켜져 있으면 저장 없이 화면 동작만 테스트)

## 참고
- 브라우저 보안 정책(CORS) 때문에 요청은 `no-cors` 모드로 전송되며, 응답 내용은 읽지 않고 "성공했다고 가정"하고 화면을 넘깁니다. 실제 기록 여부는 스프레드시트를 열어 확인하세요.
- 같은 사람이 같은 날 같은 항목(출근/퇴근)을 두 번 누르면 브라우저에 저장된 기록을 기준으로 막고 경고를 띄웁니다. 키오스크 기기를 바꾸거나 브라우저 저장공간을 지우면 이 로컬 중복 방지 기록은 초기화됩니다.
- 실제 이름 30명은 아직 플레이스홀더입니다 — 실제 명단을 주시면 반영하겠습니다.
