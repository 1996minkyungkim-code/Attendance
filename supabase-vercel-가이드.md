# Supabase + Vercel 연동 가이드

## 1. Supabase 테이블
Project: `ikktwyzjmwooohtlgmyw` (`https://ikktwyzjmwooohtlgmyw.supabase.co`)

SQL Editor에서 아래를 실행해 `attendance_records` 테이블을 만듭니다.

```sql
create table attendance_records (
  id bigint generated by default as identity primary key,
  name text not null,
  course text not null check (course in ('phd','master')),
  action text not null check (action in ('in','out')),
  date_key text not null,
  date text not null,
  time text not null,
  created_at timestamptz not null default now()
);

create unique index attendance_unique_per_day
  on attendance_records (name, action, date_key);

alter table attendance_records enable row level security;

create policy "anon can insert attendance"
  on attendance_records for insert to anon with check (true);

create policy "anon can read attendance"
  on attendance_records for select to anon using (true);
```

## 2. 클라이언트 연동
- `index.html` (= `출퇴근 키오스크.dc.html`)에서 `@supabase/supabase-js`를 CDN으로 로드하고, `supabaseUrl` / `supabaseKey` props로 `createClient`를 초기화합니다.
- `supabaseKey`는 **publishable(anon) key**입니다. RLS 정책으로 insert/select만 허용되어 있어 브라우저에 노출되어도 안전합니다. **service_role 키는 절대 클라이언트 코드에 넣지 마세요.**
- 중복 출퇴근 체크는 이제 Supabase 조회 기준이라 여러 키오스크 기기에서도 동일하게 동작합니다. DB에도 unique index가 있어 이중 방어됩니다.
- Tweaks(조정) 패널에서 `supabaseUrl` / `supabaseKey` 값을 바꿔 다른 프로젝트로 전환할 수 있습니다.

## 3. Vercel 배포 (GitHub 연동)
1. https://vercel.com/new 접속 (또는 대시보드 → Add New → Project)
2. GitHub 저장소 `1996minkyungkim-code/Attendance` Import
3. Framework Preset: **Other** (별도 빌드 없음)
4. Build Command / Output Directory: 비워둠 (정적 파일 그대로 서빙)
5. Deploy 클릭 → 완료되면 `index.html`이 루트 URL에서 서빙됩니다.
6. 이후 `main` 브랜치에 git push할 때마다 자동으로 재배포됩니다.

## 참고
- 실제 이름 30명은 아직 플레이스홀더입니다 — 실제 명단을 주시면 반영하겠습니다.
- `출퇴근 키오스크.dc.html`과 `index.html`은 동일한 내용입니다. `index.html`은 Vercel이 루트 경로에서 정적 파일로 인식할 수 있도록 추가한 파일입니다.
